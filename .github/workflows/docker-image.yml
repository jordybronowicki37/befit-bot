name: Build and Publich Docker Image

on:
  workflow_dispatch:
    inputs:
      version:
        description: The image version
        required: true
        type: string

env:
  IMAGE_NAME: befit
  REGISTRY_URL: ghcr.io

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    
    permissions:
      packages: write
      contents: read

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag $REGISTRY_URL/${{ github.actor }}/$IMAGE_NAME:${{ inputs.version }}
      working-directory: backend
    - name: Login to Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login $REGISTRY_URL -u ${{ github.actor }} --password-stdin
    - name: Publish the Docker image
      run: docker push $REGISTRY_URL/${{ github.actor }}/$IMAGE_NAME:${{ inputs.version }}
